---
description: "Compare two authors' writing styles side-by-side. Load two style profiles and generate a visual comparison report."
---

Invoke the ghost-writer skill to compare two style profiles.

1. Load two `{author}-style-profile.json` files.
2. Generate a comparison across all 24 dimensions.
3. Output a `style-comparison.html` report with:
   - Side-by-side radar charts
   - Dimension-by-dimension comparison table
   - Key differences highlighted
   - "If Author A wrote like Author B" hypothetical example
